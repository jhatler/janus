{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run lint script",
            "type": "process",
            "command": "./scripts/lint",
            "group": "test",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
