{
    "QueueUrl": "${QueueUrl}",
    "MessageBody": "{#set($allParams = $input.params())#foreach($type in $allParams.keySet())#set($params = $allParams.get($type))\"$type\":{#foreach($paramName in $params.keySet())\"$paramName\":\"$util.escapeJavaScript($params.get($paramName))\"#if($foreach.hasNext),#end#end},#end\"body\":\"$util.base64Encode($input.body)\"}",
    "MessageGroupId": "$util.escapeJavaScript($input.params('X-GitHub-Event'))"
}
